# Compare the two list

list_one = [1, 2, 5, 6, 5]
list_two = [1, 2, 5, 6, 3]

if (list_one) == len(list_two):
    print "The list are the same"

else:
    print "The list are not the same"

list_one = [1,2,5,6,2]
list_two = [1,2,5,6,2]


if (list_one) == (list_two):
    print "The list are the same"

else:
    print "The list are not the same"

